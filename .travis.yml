language: objective-c

branches:
  only:
    - master
    - rust-0.8

before_install:
  - brew update
install:
  - brew install rust wxmac
script:
  - mkdir -p build
  - cd build
  - cmake ..
  - make
